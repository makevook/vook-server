@ApplicationModule(
        type = ApplicationModule.Type.OPEN,
        allowedDependencies = {
                "common"
        }
)
package vook.server.api.domain.term;

import org.springframework.modulith.ApplicationModule;
